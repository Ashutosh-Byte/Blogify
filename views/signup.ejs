<!DOCTYPE html>
<html lang="en">
  <head>
    <%- include("./partials/head.ejs") %>
    <title>Homepage</title>
    <style>
body {
  background: linear-gradient(120deg, #f8fafc 0%, #e7eaf6 100%);
  min-height: 100vh;
  margin: 0;
  font-family: "San Francisco", "Segoe UI", "Helvetica Neue", Arial, sans-serif;
}

.container {
  max-width: 420px;
  margin: 48px auto;
}

form {
  background: rgba(255,255,255,0.65);
  border-radius: 18px;
  box-shadow: 0 8px 32px 0 rgba(31,38,135,0.13);
  backdrop-filter: blur(18px) saturate(180%);
  -webkit-backdrop-filter: blur(18px) saturate(180%);
  padding: 32px 28px 24px 28px;
  border: 1px solid rgba(255,255,255,0.18);
  transition: box-shadow 0.3s, background 0.4s, backdrop-filter 0.4s;
  animation: formBlurIn 0.7s cubic-bezier(0.4, 0, 0.2, 1);
}

@keyframes formBlurIn {
  from {
    opacity: 0;
    transform: translateY(24px) scale(0.98);
    backdrop-filter: blur(0px) saturate(100%);
    -webkit-backdrop-filter: blur(0px) saturate(100%);
  }
  to {
    opacity: 1;
    transform: translateY(0) scale(1);
    backdrop-filter: blur(18px) saturate(180%);
    -webkit-backdrop-filter: blur(18px) saturate(180%);
  }
}

form:hover {
  box-shadow: 0 12px 36px 0 rgba(31,38,135,0.16);
  background: rgba(255,255,255,0.80);
  backdrop-filter: blur(24px) saturate(200%);
  -webkit-backdrop-filter: blur(24px) saturate(200%);
}

.form-label {
  font-weight: 600;
  color: #232946;
  letter-spacing: 0.5px;
}

.form-control {
  border-radius: 10px;
  border: 1px solid #e0e0e0;
  padding: 10px 14px;
  font-size: 1rem;
  background: rgba(255,255,255,0.85);
  transition: border 0.2s, box-shadow 0.2s;
  margin-bottom: 8px;
}

.form-control:focus {
  border: 1.5px solid #4f8cff;
  box-shadow: 0 0 0 2px #e3f0ff;
  background: #fff;
}

.btn-primary {
  background: linear-gradient(90deg, #4f8cff 0%, #38b6ff 100%);
  border: none;
  border-radius: 10px;
  padding: 10px 10px;
  font-size: 1.08rem;
  font-weight: 600;
  color: #fff;
  box-shadow: 0 2px 8px rgba(79,140,255,0.10);
  transition: background 0.2s, box-shadow 0.2s;
}

.btn-primary:hover, .btn-primary:focus {
  background: linear-gradient(90deg, #2563eb 0%, #4f8cff 100%);
  box-shadow: 0 4px 16px rgba(79,140,255,0.13);
}

@media (prefers-color-scheme: dark) {
  body {
    background: linear-gradient(120deg, #1a1a2e 0%, #16213e 100%);
    color: #e0e0e0;
  }

  form {
    background: rgba(30,30,30,0.75);
    box-shadow: 0 8px 32px 0 rgba(0,0,0,0.2);
    border: 1px solid rgba(255,255,255,0.1);
  }

  form:hover {
    background: rgba(40,40,40,0.85);
    box-shadow: 0 12px 36px 0 rgba(0,0,0,0.25);
  }

  .form-label {
    color: #d1d5db;
  }

  .form-control {
    background: rgba(50,50,50,0.85);
    color: #f3f4f6;
    border: 1px solid #4b5563;
  }

  .form-control:focus {
    background: #1f2937;
    border: 1.5px solid #3b82f6;
    box-shadow: 0 0 0 2px #93c5fd;
  }

  .btn-primary {
    background: linear-gradient(90deg, #2563eb 0%, #1d4ed8 100%);
    color: #f9fafb;
  }

  .btn-primary:hover, .btn-primary:focus {
    background: linear-gradient(90deg, #1e40af 0%, #2563eb 100%);
  }
}
@media (prefers-color-scheme: dark) {
  body {
    background: linear-gradient(120deg, #181c22 0%, #232946 100%);
    color: #f3f4f6;
  }
  .container {
    background: none;
  }
  form {
    background: rgba(30,34,44,0.85);
    border: 1px solid rgba(48,54,68,0.22);
    box-shadow: 0 8px 32px 0 rgba(20,24,38,0.19);
  }
  .form-label {
    color: #f3f4f6;
  }
  .form-control {
    background: rgba(35,41,70,0.85);
    border: 1px solid #363b4f;
    color: #ffffff;
  }
  .form-control:focus {
    background: #232946;
    border: 1.5px solid #4f8cff;
    box-shadow: 0 0 0 2px #223666;
    color: #ffffff;
  }
  .form-control::placeholder {
    color: #b0b0b0;
    opacity: 1;
  }
  .btn-primary {
    background: linear-gradient(90deg, #2563eb 0%, #4f8cff 100%);
    color: #fff;
  }
}
</style>
  </head>
  <body>
    <%- include("./partials/nav.ejs") %>
    <div class="container mt-4" >
      <form action="/user/signup" method="Post">
        <div class="mb-3">
          <label for="Fullname" class="form-label"
            >Full Name</label
          >
          <input
            type="text"
            class="form-control"
            id="Fullname"
            name="Fullname"
            aria-describedby="Fullname"
          />
         


        <div class="mb-3">
          <label for="exampleInputEmail1" class="form-label"
            >Email address</label
          >
          <input
            type="email"
            class="form-control"
            id="exampleInputEmail1"
            aria-describedby="emailHelp"
             name="Email"
          />
          <div id="emailHelp" class="form-text">
            We'll never share your email with anyone else.
          </div>
        </div>
        <div class="mb-3">
          <label for="exampleInputPassword1" class="form-label">Password</label>
          <input
            type="password"
            class="form-control"
            id="exampleInputPassword1"
            name="Password"
          />
        </div>

        <button type="submit" class="btn btn-primary">Submit</button>
      </form>
    </div>
    <%- include("./partials/script.ejs") %>
  </body>
</html>
